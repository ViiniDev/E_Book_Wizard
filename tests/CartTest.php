<?php
namespace tests;
require 'vendor/autoload.php';
require "Core/App.php";
require "Models/Cart.php";

use PHPUnit\Framework\TestCase;
use Models\Cart;
use Core\App;
use Core\Database;
use Models\User;

class CartTest extends TestCase
{
    public function test_add_to_cart()
    {
 
       // $userId = 26;
       // $ebookId=3;
       // $quantity=1;
       // $ebookTitle='O Senhor dos Anéis: As duas torres';
       // $ebookPrice= 52.43;
    
        
       // $cart = new Cart;
        //$this->assertTrue($cart->addToCart($userId, $ebookId, $quantity, $ebookTitle, $ebookPrice));
       // $this->assertTrue($cart->addToCart(26, 3, 1, 'O Senhor dos Anéis: As duas torres', 52.43));

          $this->assertTrue(true);
    }
}

//MINUTO 22:36
